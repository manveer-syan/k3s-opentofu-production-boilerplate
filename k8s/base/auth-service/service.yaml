apiVersion: v1
kind: Service
metadata:
  name: auth-service-svc
  namespace: manveersyan-group
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
      name: http
  selector:
    app: auth-service
